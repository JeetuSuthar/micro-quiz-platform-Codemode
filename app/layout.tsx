import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Micro-Quiz Platform",
  description: "Test your knowledge with short, engaging quizzes across various topics",
  keywords: "quiz, education, learning, test, knowledge, history, science, math, programming",
  authors: [{ name: "Micro-Quiz Platform" }],
  viewport: "width=device-width, initial-scale=1",
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
